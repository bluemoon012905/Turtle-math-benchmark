# Turtle-math-benchmark
some basic calculation testing

Static browser game for GitHub Pages.

Open `index.html` locally to test, or publish the repository with GitHub Pages to host it as a static site.
